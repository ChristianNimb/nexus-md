export { core$session$id$encodeSession as encodeSession, core$session$id$formatShortId as formatShortId, core$session$id$hasLocalSession as hasLocalSession, core$session$id$markBootState as markBootState, core$session$id$redeemSession as redeemSession, core$session$id$shortId as shortId, core$session$id$shouldIssueSession as shouldIssueSession } from '../_core/core.js';
